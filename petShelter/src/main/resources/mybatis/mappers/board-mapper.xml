<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boardMapper">

 	<resultMap type="Board" id="boardResultSet">
		<result column="BOARD_NO" property="boardNo"/>
		<result column="BOARD_TITLE" property="boardTitle"/>
		<result column="MEMBERNO" property="memberNo"/> <!-- 추가 -->
		<result column="FILENO" property="fileNo"/> <!-- 추가 -->
		<result column="CATEGORY_NO" property="categoryNo"/> <!-- 추가 -->
		<result column="BOARD_CONTENT" property="boardContent"/>
		<result column="COUNT" property="count"/>
		<result column="RECOMMEND" property="recommend"/> <!-- 추가 -->
		<result column="UPLOAD_DATE" property="uploadDate"/>
		<result column="REVISE_DATE" property="reviseDate"/>
		<result column="STATUS" property="status"/>
	</resultMap>

	<select id="listCount" resultType="int">
		SELECT COUNT(*)
		FROM BOARD
		WHERE STATUS = 'Y'
	</select>
	
	<select id="selectList" resultType="Board">
		SELECT BOARD_NO
		      ,BOARD_TITLE
		      ,MEMBERNO
		      ,COUNT
		      ,UPLOAD_DATE
		FROM BOARD
		WHERE STATUS = 'Y'
		ORDER BY UPLOAD_DATE DESC
	</select>
	
	<select id="cListCount" parameterType="int" resultType="int">
		SELECT COUNT(*)
		FROM BOARD
		WHERE CATEGORY_NO = #{categoryNo}
		AND STATUS = 'Y' 
	</select>

	<select id="orderByCategory" parameterType="int" resultMap="boardResultSet">
		SELECT BOARD_NO
		      ,BOARD_TITLE
		      ,MEMBERNO
		      ,COUNT
		      ,UPLOAD_DATE
		FROM BOARD
		WHERE CATEGORY_NO = #{categoryNo}
		AND STATUS = 'Y'
		ORDER BY UPLOAD_DATE DESC
	</select>
	
	<update id="increaseCount" parameterType="int">
		UPDATE BOARD
		SET COUNT = COUNT + 1
		WHERE BOARD_NO = #{boardNo}
		AND STATUS = 'Y'
	</update>
	
	<select id="selectBoard" parameterType="int" resultType="Board">
		SELECT BOARD_NO
			  ,BOARD_TITLE
			  ,BOARD_CONTENT
			  ,MEMBERNO
			  ,UPLOAD_DATE
			  ,REVISE_DATE
			  ,COUNT
			  ,RECOMMEND
		FROM BOARD 
		WHERE BOARD_NO = #{boardNo}
		AND STATUS = 'Y'
	</select>
	
	
	 <insert id="insertBoard" parameterType="Board">
		INSERT INTO BOARD(BOARD_NO
		                 ,BOARD_TITLE
		                 ,MEMBERNO
		                 ,CATEGORY_NO
		                 ,BOARD_CONTENT
		                 ,FILENO
		                 ,UPLOAD_DATE
		                 ,REVISE_DATE
		                 ,STATUS)
        		VALUES (SEQ_BNO.NEXTVAL
               			,#{boardTitle}
               			,#{memberNo}
               			,#{categoryNo}
               			,#{boardContent}
               			,#{fileNo}
               			,SYSDATE
               			,SYSDATE
               			,'Y')
	</insert>
</mapper>
